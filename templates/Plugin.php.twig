<?php
/**
 * Phergie (http://phergie.org)
 *
 * @link {{github_url}} for the canonical source repository
 * @copyright Copyright (c) 2008-{{ "now"|date("Y") }} Phergie Development Team (http://phergie.org)
 * @license http://phergie.org/license New BSD License
 * @package {{namespace}}
 */

namespace {{namespace}};

use Phergie\Irc\Bot\React\AbstractPlugin;
use Phergie\Irc\Bot\React\EventQueueInterface;
use {{event_class_full}};

/**
 * Plugin for {{purpose}}.
 *
 * @category Phergie
 * @package {{namespace}}
 */
class Plugin extends AbstractPlugin
{
    /**
     * Accepts plugin configuration.
     *
     * Supported keys:
     *
     *
     *
     * @param array $config
     */
    public function __construct(array $config = array())
    {

    }

    /**
     *
     *
     * @return array
     */
    public function getSubscribedEvents()
    {
        return array(
            '{% if command_plugin %}command{% else %}irc{% endif %}.' => '{{handler_method}}',
        );
    }

    /**
     *
     *
     * @param \{{event_class_full}} $event
     * @param \Phergie\Irc\Bot\React\EventQueueInterface $queue
     */
    public function {{handler_method}}({{event_class_short}} $event, EventQueueInterface $queue)
    {
    }
}
