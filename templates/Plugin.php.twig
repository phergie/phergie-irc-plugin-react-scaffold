<?php
/**
 * Phergie plugin for {{purpose}} ({{plugin_url}})
 *
 * @link {{repo_url}} for the canonical source repository
 * @copyright Copyright (c) {{copyright_years}} {{author_name}} ({{author_url}})
 * @license {{license_url}} {{license_name}}
 * @package {{package_namespace}}
 */

namespace {{package_namespace}};

use Phergie\Irc\Bot\React\AbstractPlugin;
use Phergie\Irc\Bot\React\EventQueueInterface as Queue;
use {{event_class_full}} as Event;

/**
 * Plugin class.
 *
 * @category {{vendor_namespace}}
 * @package {{package_namespace}}
 */
class Plugin extends AbstractPlugin
{
    /**
     * Accepts plugin configuration.
     *
     * Supported keys:
     *
     *
     *
     * @param array $config
     */
    public function __construct(array $config = array())
    {

    }

    /**
     *
     *
     * @return array
     */
    public function getSubscribedEvents()
    {
        return array(
            '{% if command_plugin %}command{% else %}irc{% endif %}.' => '{{handler_method}}',
        );
    }

    /**
     *
     *
     * @param \{{event_class_full}} $event
     * @param \Phergie\Irc\Bot\React\EventQueueInterface $queue
     */
    public function {{handler_method}}(Event $event, Queue $queue)
    {
    }
}
